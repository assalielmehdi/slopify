{
  "name": "@loop/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/server.js",
  "types": "./dist/app.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run --filter @loop/clickup-artifacts build && bun run --filter @loop/execution-runtime build && bun ../../node_modules/typescript-go/bin/tsc --project tsconfig.json",
    "typecheck": "bun run --filter @loop/clickup-artifacts build && bun run --filter @loop/execution-runtime build && bun ../../node_modules/typescript-go/bin/tsc --project tsconfig.json --noEmit",
    "lint": "bun --bun eslint src tests --max-warnings 0",
    "test": "bun --bun vitest run --root ../.. apps/api/tests",
    "start": "bun dist/server.js"
  },
  "dependencies": {
    "@loop/clickup-artifacts": "workspace:*",
    "@loop/contracts": "workspace:*",
    "@loop/execution-runtime": "workspace:*",
    "@loop/workflow-model": "workspace:*",
    "hono": "4.13.3",
    "zod": "4.4.3"
  }
}
