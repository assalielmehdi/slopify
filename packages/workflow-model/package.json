{
  "name": "@loop/workflow-model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "source": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run --filter @loop/contracts build && bun ../../node_modules/typescript-go/bin/tsc --project tsconfig.json",
    "typecheck": "bun run --filter @loop/contracts build && bun ../../node_modules/typescript-go/bin/tsc --project tsconfig.json --noEmit",
    "lint": "bun --bun eslint src tests --max-warnings 0",
    "test": "bun --bun vitest run --root ../.. packages/workflow-model/tests"
  },
  "dependencies": {
    "@loop/contracts": "workspace:*",
    "zod": "4.4.3"
  }
}
