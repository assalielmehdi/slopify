{
  "name": "@loop/agent-runtimes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "source": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run --filter @loop/workflow-model build && bun ../../node_modules/typescript-go/bin/tsc --project tsconfig.json",
    "typecheck": "bun run --filter @loop/workflow-model build && bun ../../node_modules/typescript-go/bin/tsc --project tsconfig.json --noEmit",
    "lint": "bun --bun eslint src tests --max-warnings 0",
    "test": "bun --bun vitest run --root ../.. packages/agent-runtimes/tests",
    "test:integration": "bun --bun vitest run --root ../.. packages/agent-runtimes/tests/integration"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.84.2",
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@loop/contracts": "workspace:*",
    "@loop/workflow-model": "workspace:*",
    "typebox": "1.3.7",
    "zod": "4.4.3"
  }
}
