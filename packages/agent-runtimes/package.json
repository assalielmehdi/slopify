{
  "name": "@loop/agent-runtimes",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "source": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @loop/workflow-model build && tsc --project tsconfig.json",
    "typecheck": "pnpm --filter @loop/workflow-model build && tsc --project tsconfig.json --noEmit",
    "lint": "eslint src tests --max-warnings 0",
    "test": "vitest run --root ../.. packages/agent-runtimes/tests",
    "test:integration": "vitest run --root ../.. packages/agent-runtimes/tests/integration"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.84.2",
    "@earendil-works/pi-coding-agent": "0.84.2",
    "@loop/contracts": "workspace:*",
    "@loop/workflow-model": "workspace:*",
    "typebox": "1.3.7",
    "zod": "4.4.3"
  }
}
